"""
Modelos para la aplicación dashboard.

Nota: Aunque no se requiere persistencia en base de datos para esta prueba técnica,
esta es la estructura estándar donde se definirían los modelos Django.
"""

from django.db import models

# Create your models here.
# Los modelos se definirían aquí si fuera necesario persistir datos.
# Para esta aplicación, usamos datos simulados en memory (ver data.py).
